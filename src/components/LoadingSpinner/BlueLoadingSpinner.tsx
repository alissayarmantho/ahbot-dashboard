const BlueLoadingSpinner: React.FC = () => {
  return (
    <div className="w-16 h-16 border-4 border-blue-400 border-dotted rounded-full animate-spin self-center"></div>
  );
};

export default BlueLoadingSpinner;
